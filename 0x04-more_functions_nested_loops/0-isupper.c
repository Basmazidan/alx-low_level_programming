#include "main.h"

/**
 * _isupper - checks if c is upper
 *
 * @c: input for alphabet
 *
 * Return 1 if its uppercase and 0 if is not
*/

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
		return (i);
	else
		return (0);
}
